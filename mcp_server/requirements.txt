# Minimal deps for MCP email server
# Install in a virtual environment for local use with Claude Desktop or other MCP clients
sqlalchemy>=2.0
asyncpg>=0.29
passlib>=1.7
httpx>=0.27
# Pick ONE of these MCP SDKs depending on your client. Claude Desktop supports the official MCP SDK.
# Official Python MCP SDK
mcp>=0.1.0
